// Tipagem para o estado do pagamento
export type StatePayment = "Aprovado" | "Pendente" | "Recusado"

// Tipagem para o estado do cliente da empresa
export type StateClient = "Ativo" | "Pendente" | "Desativo"